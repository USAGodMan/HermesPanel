# ==============================================================================
# Hermes Project - 环境变量模板文件 (.env.example)
#
# 使用方法:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 编辑 .env 文件，并填写所有必需的值。
#
# 【警告】请不要将包含真实密码和密钥的 .env 文件提交到任何公共 Git 仓库！
# ==============================================================================

# --- 域名配置 ---
# 【必需】填写您的主域名，例如：hermes.example.com
# 确保该域名的 A 记录已指向此服务器的公网 IP。
NGINX_HOST=

# --- 安全密钥 ---
# 【必需】在生产环境中，请务必使用强随机值替换以下默认值。
# 您可以使用 'openssl rand -base64 32' 命令来生成。
JWT_SECRET_KEY=replace_this_with_a_strong_random_jwt_secret_key
AES_ENCRYPTION_KEY=replace_this_with_a_strong_32_byte_aes_key

# --- 服务端口 (通常无需修改) ---
HTTP_PORT="8080"
GRPC_PORT="50051"

# --- Gin 运行模式 ---
# 'release' 用于生产环境, 'debug' 用于开发
GIN_MODE="release"

# ==============================================================================
# 数据库配置
# ==============================================================================

# --- 数据库类型 ---
# 【必需】选择您要使用的数据库类型。可选值为 "mysql" 或 "sqlite"。
# 对于初次体验或轻量级部署，推荐使用 "sqlite"。
DB_TYPE=mysql

# --- MySQL 配置 ---
# 【注意】只有当 DB_TYPE="mysql" 时，以下变量才有效。

# [必需] MySQL root 用户的密码，仅用于 mysql 容器首次初始化。
MYSQL_ROOT_PASSWORD=replace_this_with_a_strong_mysql_root_password

# [可选] Hermes 应用要使用的数据库名 (默认为 hermes)
MYSQL_DATABASE=hermes_db

# [可选] Hermes 应用连接数据库时使用的用户名 (默认为 hermes)
MYSQL_USER=hermes_user

# [必需] Hermes 应用连接数据库时使用的密码。
MYSQL_PASSWORD=replace_this_with_a_strong_mysql_user_password

# --- SQLite 配置 ---
# 【注意】只有当 DB_TYPE="sqlite" 时，以下变量才有效。

# [可选] SQLite 数据库文件的路径 (相对于容器内部)。
# 如果留空，将默认使用 "data/hermes.db"。
# DB_DSN=data/hermes.db
